[project]
name = "fog-node"
version = "0.1.0"
description = "Fog Node TTS for distributed audiobook generation"
requires-python = ">=3.8"

[tool.ruff]
line-length = 88
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I"]
